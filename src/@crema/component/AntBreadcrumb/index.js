import { Breadcrumb } from 'antd';
import React from 'react';

export const AntBreadcrumb = (props) => {
  return <Breadcrumb {...props} />;
};

export const AntBreadcrumbItem = (props) => {
  return <Breadcrumb.Item {...props} />;
};
