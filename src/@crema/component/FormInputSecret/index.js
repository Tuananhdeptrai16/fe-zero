import wrapperFormItem from 'src/HOC/wrapperFormItem';
import InputSecret from 'src/@crema/component/InputSecret';

const FormInputSecret = wrapperFormItem(InputSecret);

export default FormInputSecret;
